using UnityEngine;

public static class IMarchingCubes {
  
    //public static MeshData CalculateMeshData(float[][][] voxels, float voxelSize) {

    //    int effLen = voxels.Length - 1;

    //    float xCoord, yCoord, zCoord;
    //    float[] valueCache = new float[4];

    //    for(int z = 1; z < effLen; ++z) {
    //        zCoord = z * voxelSize;
    //        for(int y = 1; y < effLen; ++y) {
    //            yCoord = y * voxelSize;

    //            valueCache[0] = voxels[voxelSize][yCoord * voxelSize][zCoord];

    //            for(int x = 1; x < effLen; ++x) {

    //            }
    //        }
    //    }

    //}



}
